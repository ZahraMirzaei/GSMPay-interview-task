/**
 * Todo: get this `totalTasks` and `doneTasks` from modal layer.
 */
export const useTodoReport = () => {
  /* ------------------------------ Dependencies ------------------------------ */
  const totalTasks = 65;
  const doneTasks = 54;
/* -------------------------------------------------------------------------- */
  return {totalTasks, doneTasks}
}